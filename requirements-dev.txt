# Dev-only tooling for the story lifecycle gate (ruff + pytest).
# Install into a repo-local venv (Debian trixie is externally-managed):
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements-dev.txt
ruff==0.16.3
pytest==9.1.1
# App runtime deps exercised by tests.
PyYAML==6.0.3
requests==2.33.0
# MCP SDK for the repo-owned knowledge connector (story-64 C2); spec pins the 1.x line.
mcp>=1.9.0,<2.0.0
# DB driver for the knowledge DB-layer tests (story-65). They auto-skip when no Postgres is
# reachable (e.g. CI), so this only enables running them where a database exists (local / the Pi).
psycopg2-binary>=2.9,<2.10
